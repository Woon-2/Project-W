- bindless resource
  - gpu command 기록 횟수 최적화 (CPU->GPU 데이터 전송 줄임)
- batch rendering
  - batch를 구성할 때 직접 구현한 timsort 활용
    - 씬을 초기화할 때 제외 batch는 항상 거의 정렬된 상태
    - std::sort는 introsort기반 구현으로, 재귀 깊이에 비례하여 데이터를 읽음.     
    이상적인 경우에도 log(N) 번의 중복 읽기는 발생
    - timsort로 하게 되면 run 생성 단계에서 굉장히 적은 수의 run이 만들어짐    
    동일한 종류의 객체를 연속해서 batch에 집어넣을 경우가 많으므로,     
    삽입 시 객체를 batch의 맨 앞에 넣어도 run의 수가 많지 않을 것이 기대됨
- gpu memory pool
  - vertex buffer와 index buffer, 그리고 반드시 매 드로우콜마다 바인딩되어야 하는 데이터의 경우     
  다 별도로 CreateCommittedResource로 만들게 되면 씬의 로딩 시간이 눈에 띄게 느려진다.
  - D3D12_GPU_VIRTUAL_ADDRESS만으로 gpu에 리소스를 올릴 수 있는 경우,    
  한 번의 CreateCommittedResource로 할당된 메모리를 임의로 분할하여 사용하도록 한다.
    - Placed Resource는 공부해보니 union과 유사했다.     
    마법처럼 메모리를 분할해주는 게 아니라 시점에 따라 뭐가 들어있을지 모르는 메모리에 가까웠다.
- LOD(카디널 스플라인, 업데이트 주기)
  - 객체 수가 많고, 필요한 충돌 검사량이 많다.     
  - 플레이어의 시야에 잘 안 보이는 객체들은 업데이트 주기를 더 길게한다.
    - 객체 전체를 감싸는 바운딩 박스의 꼭짓점들 중 플레이어와 가장 가까운 점 기준 계산
  - 렌더링 시 LOD는 gpu에서 카메라와의 거리에 기반해 계산, 카디널 스플라인을 이용해 Tessealtion한다.   
    - 지형이나 모델을 Tesselation할 때 제어점이 필요한데,     
    Control patch 기반 모델을 구하기도 쉽지않고 우리는 모델러도 없다.     
    따라서 메시의 정점을 그대로 제어점으로 사용, 제어점을 뚫고 지나가는 스플라인을 이용해야 한다.
- 조명 컬링 & defered shading
  - 1학기 3D게임프로그래밍1 과제에서, 파티클을 렌더링할 시 gpu 소요 시간이 매우 긴 것으로 관측됐는데,     
  그 이유는 조명 계산이었다.
  - defered shading을 통해 보이는 픽셀에 대해서만 조명 연산을 수행할 수 있도록 하고,
  분할된 공간마다 영향을 줄 조명들의 배열을 새로 세팅하여 조명을 계산한다.
    - 오브젝트마다 영향을 미치는 조명을 검색하지 않는 이유는,     
    배열을 처음부터 끝까지 다시 구성하는 연산이 가볍지 않기 때문에 그 횟수를 줄이기 위해서이다.
- PBR & IBL & HDR
- 거대 지형 분할 로딩
  - 우리 게임은 스테이지가 몇 개의 방으로 나누어져 있는 것이 아니라 쭉 이어져있다.     
  지형을 분할해서 로딩하여 상주 gpu 메모리를 줄이도록 한다.
- 모델 익스포터(BVH)
  - 유니티를 익스포터로 써서 모델 파일을 게임에서 로드할 수 있는 형태로 가공하는데,     
  Bounding Volume Hierarchy를 기록할 수 있도록 한다.
  - texture remapping 정보를 파일로 입력받아 모델 내부의 텍스처의 파일명이나    
  리소스 타입(Texture2D, TextureArray, TextureCube)를 수정할 수 있도록 한다.
- gpu 파티클 이펙트
  - 그림은 기하 셰이더를 활용하여 그리고, 업데이트는 컴퓨트 셰이더를 활용하여 한다.
- C++17 pmr
  - 게임의 메모리 참조 패턴을 분석하여 참조 지역성이 향상되도록 메모리를 배치하고,     
  할당 및 해제의 비용을 줄인다.
- (C++17 pmr 라이브러리와 연동되는 Lock Free 자료구조)
- (분할된 공간에서 각 공간의 시뮬레이션을 Task로 삼아 병렬 처리하는 Task 기반 멀티스레딩)
- thread unsafe shared_ptr
  - 소멸자의 가상 함수 호출을 제거하고 aliasing 기능을 보존하여     
  인라인 확률을 높이고 간접 참조 횟수를 줄인다.
  - thread safety를 제거하여 그러한 요구사항이 필요하지 않은 곳에서의 오버헤드를 줄인다.
- 트리플 버퍼링, SwapChain의 Waitable Object
  - gpu가 화면에 그림을 그리고 있는 와중에도 다른 백버퍼에 그림을 그릴 수 있도록 해,    
  gpu의 사용률과 프레임레이트를 높인다.
  - 파이프라인에서 write가 일어나는 리소스의 경우 중첩이 필요하다.
  - Command List 또한 write가 일어난다.     
  gpu가 그림을 그릴 때 command list를 읽는데, 우리가 동일한 command list 객체로 명령을 기록하면 오류가 난다.     
  즉 command list도 여러 개를 써야 한다.
- 스플라인 카메라
  - 우리 게임은 부드러운 장면 전환에 스플라인 카메라를 사용한다.
- 데이터베이스 서버 연동