## 캐릭터 성장

### 레벨 시스템
- 캐릭터는 레벨업을 할 때마다 활용할 수 있는 스탯 포인트 4개가 생긴다.
  - 바로 스탯을 찍지 않고 추가적으로 레벨업을 하길 기다려도 된다.
  - 현재 까지 찍은 스탯 수에 비례한 인게임 재화를 지불하여 모든 스탯을 초기화할 수 있다.
  - 찍을 수 있는 스탯은 `체력`, `민첩`, `지능`의 세 가지이다.
- 레벨 x에서 x+1로 레벨업하는데 필요한 경험치 y는    
  `y = 500*x^2+1000`을 따른다.
- 게임의 최고 레벨은 50이다.

### 스탯
- 무기 숙련도: 데미지 배율에 영향 (비 레벨업 스탯)
  - 검, 창, 도끼, 활, 완드의 다섯 가지 무기 종류 각각에 대한 숙련도
  - 특정 종류의 무기들을 얼마나 많이 숙달했는가를 나타내는 지표
  - 플레이어의 최종 주무기 숙련도 예상치 1000
    - 최초 데미지 배율 x0.5에서 최종 x1.28이 되는 설계
    - 무기 숙련도 400에서 데미지 배율 x1.0 달성
- 체력: 장비를 들 수 있는 무게, 최대 체력에 영향
- 민첩: 공격 속도, 회피율, 적중률에 영향
- 지능: 무기 숙련도 및 무기 경험치 성장 속도에 영향

## 장비

### 무기
무기는 사용하면 사용할수록 해당 무기에 대한 경험치가 쌓여 데미지가 증가하게 되고,      
일정량 이상의 경험치가 쌓이면 무기 스킬이 개방된다.

무기의 스탯은 다음과 같다.

- 무기 난이도: 무기 경험치 성장 속도를 감소
- 무기 경험치: 데미지 배율에 영향
  - 무기 경험치 최대치 300
    - 최초 데미지 배율 x0.5에서 최종 x1.8이 되는 설계
    - 무기 경험치 100에서 데미지 배율 x1.0 달성
- 공격력 범위: 최소 데미지와 최대 데미지로 표현
- 공격 속도: 초당 공격 횟수로 표현
- 무게
- 적중률 보너스

## 방어구
착용 가능한 방어구의 종류로는 머리 방어구, 몸통 방어구, 손 방어구, 다리 방어구가 있다.    
모든 방어구는 하나씩만 착용 가능하다.

방어구의 스탯은 다음과 같다.

- 머리 방어력
- 몸통 방어력
- 다리 방어력
- 팔 방어력

## 데미지 계산

피격 판정이 있을 시 데미지 계산에 앞서, `회피 여부`가 먼저 계산된다.     
(1 + 공격자의 `적중률` - 피격자의 `회피율`)의 확률로 공격이 적중할 수 있고,     
오직 적중한 경우에만 다음 단계의 피해량을 계산 후 적용한다.

`무기 숙련도`와 `무기 경험치`를 반영하여 다음과 같이 `감소 전 피해량 배율(%)`가 산정된다.     
`감소 전 피해량 배율(%)` = [ { `무기 경험치` * sqrt(`무기 경험치`) / (`무기 경험치` + 100) } * 10 + 50 ] * { sqrt(`무기 숙련도`) * 5 / 2 + 50 }

그리고 무기의 `공격력 범위` 내에서 `판정 공격력`을 랜덤하게 산정하는데, 다음과 같이 한다.     
`0 <= x <= 1`에서 정의된 함수 `y = -2x(x-1) + 2/3`를 확률밀도함수로 삼는다.     
뽑힌 확률변수 `x`를 `최소 데미지`과 `최대 데미지` 사이의 선형보간 계수로 삼아 `판정 공격력`을 구한다.     
즉, `판정 공격력` = `x` * `최소 데미지` + (1 - `x`) * `최대 데미지`이다.

몬스터나 캐릭터는 바운딩 박스별로 데미지 가중치가 부여되어 있다.      
이를 `피격 부위 데미지 계수`라고 한다.     
인간형인 경우 부위별로 `피격 부위 데미지 계수`는 다음과 같이 적용된다.
- 말단(손, 발): 0.2
- 사지(팔, 다리): 0.5
- 몸통(상체, 하체): 1.0
- 머리: 1.2

`감소 전 피해량` = (`감소 전 피해량 배율(%)` / 100) * `판정 공격력`,    
`실제 피해량` = [ max{ (`감소 전 피해량` - `피격자 방어력`) * 0.9, 0 } + `감소 전 피해량` * 0.1 ] * `피격 부위 데미지 계수`

### 입힌 데미지에 따른 성장

이때, `실제 피해량`에 비례하여 `무기 숙련도`와 `무기 경험치`가 상승하는데, 공식은 다음과 같다.    
`무기 숙련도 상승치` = `실제 피해량` / `캐릭터 레벨` * k * (`지능` + 10) * 10 / (`현재 무기 숙련도` + 200)     
`무기 경험치 상승치` = `실제 피해량` / `캐릭터 레벨` * k' * (`지능` + 5) * 2 / `무기 난이도`

처치 관여를 하면 모든 기여자에게 `캐릭터 경험치`를 고르게 분배하지만,    
`무기 숙련도`와 `무기 경험치`는 `실제 피해량`에 비례하므로 처치 관여도가 높을수록 많이 상승하게 된다.     
이는 파티로 하여금 좋은 장비를 얻었으나 아직 `무기 경험치`가 낮은 멤버가 더 많이 딜할 수 있는 환경을 조성해     
성장을 앞당길 수 있도록 하는 효과를 가져온다.

